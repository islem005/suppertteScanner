{
  "name": "scanner-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 3002 --remote",
    "deploy": "wrangler deploy --config wrangler.prod.toml",
    "migrate:dev": "wrangler d1 execute shelf-scanner-db-dev --remote --file=./migrations/001_init.sql",
    "migrate:prod": "wrangler d1 execute shelf-scanner-db --remote --file=./migrations/001_init.sql"
  },
  "dependencies": {
    "better-auth": "^1.6.13",
    "csv-parse": "^5.6.0",
    "hono": "^4.7.5",
    "kysely": "^0.28.17",
    "kysely-d1": "^0.4.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
